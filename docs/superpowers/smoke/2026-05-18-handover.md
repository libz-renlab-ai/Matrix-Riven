# 2026-05-18 launch-day handover

服务器跑在 **http://127.0.0.1:18937** （LLM_ENABLED=false，空 collector dir）。
端到端冒烟跑了，每个功能都过了，可以人工测试。

下面这份打开浏览器逐个走一遍即可；每条都标了「点开看什么」+「应该看到什么」。

---

## 0. 一行启动 / 重启

```bash
# 已经在跑（PID 见 .smoke-cache/handover.log）。要重启：
# 1. 关掉现在的：netstat -ano | findstr :18937 → Stop-Process -Id <pid>
# 2. 重启（不需要 token，本机 demo + 空数据）：
PORT=18937 HOST=127.0.0.1 LLM_ENABLED=false \
  RIVEN_COLLECTOR_DIR="$HOME/.riven/collector" \
  node packages/collector-server/dist/bin-prod-server.cjs

# 想测带 token 的 LAN 版本：
PORT=18937 HOST=127.0.0.1 LLM_ENABLED=false \
  RIVEN_AUTH_TOKEN=$(openssl rand -hex 32) \
  RIVEN_COLLECTOR_DIR="$HOME/.riven/collector" \
  node packages/collector-server/dist/bin-prod-server.cjs
# token 设了后，/landing 和 /sources 仍公开；/overview /people /projects /retro
# /api/* 都要 Authorization: Bearer <token>
```

---

## 1. Marketing flow (公开页，不要 token)

| 操作 | 期望 |
|---|---|
| 浏览器开 http://127.0.0.1:18937/landing | hero "不再看一堆 0 会话…"，6 张特性卡，底部三链接：实时看板 / 本周回顾 / 数据来源；hero lead 写 "**16** 个信号检测器" |
| 点 "查看 Demo 看板 →" | 跳 /overview?demo=1，看到完整 4 成员 / 3 项目 / 2 attention / 5 highlights / 1 collab |
| 点 "直连真实数据 →"（footer 链接） | 跳 /overview（无数据时显示空状态 "今天，团队 一切顺利"） |
| 点 footer "数据来源" → /sources | 6 条数据来源表 + 16 条信号检测器表（ID 1-16 紧凑无空缺） + 4 条 "不做什么" 边界 |

**主题切换**：右上角 ◐ 按钮，点一次切深色，再点切回浅色。刷新 / 跳页保持选择（localStorage）。

---

## 2. Demo dashboard (`/overview?demo=1`)

| 区域 | 点开看什么 | 期望 |
|---|---|---|
| Hero | "今天 2 项需关注 · 2 人高产" | 数字与下面 attention 数 + highOutput.count 对齐 |
| 4 KPI 卡 | 需关注 2 项 / 高产出 2 人 / 今日消耗 $4.32 / 整体节奏 稳 | 每张卡的小波形是占位（landing 已诚实写明 "v0.2 接真数据"） |
| Attention 行 | "blake 疑似卡住 · 卡在 status/page.tsx" + "devops-pipelines 单点依赖 · casey 一人独撑 · 今天无人推进" | 点不开（attention 行是只读 callout，无 onclick） |
| 成员 tile (4 个) | alex 推进中 · blake 卡住 · casey 推进中 · dana 节奏缓 | 点 tile → 右侧 slideover 打开 |
| 项目行 (3 个) | matrix-riven (5d eta) · team-graph (8d) · devops-pipelines 单点 | 点行 → slideover 打开 |
| 近期关键进展 | 5 条 highlights，从 commit / push / release / PR | 时间排序倒序 |
| 协作热区 | "src/leadership/views/_overview-fragments.ts" alex+blake | 1 条 |

### 2a. 成员 slideover（关键交互）

点 4 个成员 tile，应该看到的 callout 大致：

- **alex**：「本周聚焦 overview 仪表盘 / 已交付 hero 与 KPI 卡片骨架」+ 焦点 matrix-riven + 状态 推进中 + 节奏 逐渐放缓
- **blake**：「本周聚焦 status/page.tsx 报错 / 卡在 类型推导」+ 焦点 matrix-riven + 状态 卡住 + 节奏 逐渐放缓
- **casey**：「本周聚焦 team-graph 视图渲染 / 已交付 attention 编辑卡联动；修 SVG 抖动中」+ 焦点 team-graph + 状态 推进中
- **dana**：「本周聚焦 文档同步 / 多日无新会话」+ 焦点 team-graph + 状态 节奏缓

**关键校验**：每个成员的 callout / 状态 / topProject 都该跟 tile 上的 sub-label 一致。drawer 顶端有头像 + 名字 + warnings；下方是 stats / evolve / projects 三块。

### 2b. 项目 slideover

点 3 个项目行：

- **matrix-riven**：lastTouch=src/leadership/views/_overview-fragments.ts，2/2 人活跃，phase=implement
- **team-graph**：lastTouch=src/graph/render.ts，1/2 人活跃，phase=debug
- **devops-pipelines**：lastTouch=.github/workflows/ci.yml，0/1 人活跃 + bus-factor warning，今日 todayFiles 为空

drawer 关键文件 / 最近文件应该跟 lastTouch 同一项目（不会出现点了 devops 却显示 leadership 视图文件那种 bug）。

### 2c. Slideover 收尾

| 操作 | 期望 |
|---|---|
| ESC 键 | drawer 关闭 |
| 点 drawer 外部（scrim） | drawer 关闭 |
| 关掉再开另一个 | 数据正确切换，不会显示上一个的 stale 数据 |
| 等 30s 不动 | drawer 内容静默 refresh（不会闪烁/不会变空） |

---

## 3. Demo 的其他 tab

`/overview?demo=1` 上点 nav 的 People / Projects / Retro / Activity / Insights：

| Tab | 期望 |
|---|---|
| People (`/people?demo=1`) | 4 个成员 tile 完整网格，跟 overview 同一组数据 |
| Projects (`/projects?demo=1`) | 3 个项目行 |
| Retro (`/retro?demo=1`) | 本周交付 5 行 / 需要看一眼 2 行 / 突出表现 / 沉睡项目 (devops) |
| Activity (`/activity?demo=1`) | "尚未实现 · 计划在 Phase 3 上线"（tab label 已写「· soon」预告） |
| Insights (`/insights?demo=1`) | 同上 |

**关键校验**：nav URL 自动携带 `?demo=1`（无需手动加）。30s 后页面不会因为 polling 而变成空数据。

---

## 4. 真实数据空状态（不带 demo）

`/overview` `/people` `/projects` `/retro`：当前 collector 是空的，所以会显示：
- "今天，团队 一切顺利。没有需要立即处理的事。"
- "0 位成员 · 0 个项目"
- 各区域空状态文案（attention/members/projects/highlights 都各自有 placeholder）

这是预期行为——install 后没数据就是这副样子。如果想跑真实数据：把 transcripts 放进 `$HOME/.riven/collector/<email>/<date>/<sid>.json` 后刷新页面。

---

## 5. JSON API（开发者 / 自动化用）

```bash
curl http://127.0.0.1:18937/api/overview          # 空 collector，仅 schema
curl http://127.0.0.1:18937/api/overview?demo=1   # 完整 demo snapshot + _html
curl http://127.0.0.1:18937/api/llm/status        # {enabled:false}（LLM off）
curl http://127.0.0.1:18937/api/members/alex?demo=1
curl http://127.0.0.1:18937/api/projects/matrix-riven?demo=1
```

`/api/members/<id>` 既接受 local-part (`alex`) 也接受 full email (`alex@example.com`)。

---

## 6. 安全 / 健壮性

| 测试 | 期望 |
|---|---|
| `curl -X POST http://127.0.0.1:18937/<any-leadership-route>` | 405 + `nosniff` / `DENY` / `no-referrer` 头 |
| `curl 'http://127.0.0.1:18937/overview?range=foo'` | 400 `{"error":"invalid_range","allowed":[…]}` |
| `curl http://127.0.0.1:18937/api/members/nonexistent` | 404 `{"error":"not_found"}` |
| `curl http://127.0.0.1:18937/random-path` | 404 `not handled` |
| 默认绑定 | 127.0.0.1（不暴露 LAN）；想暴露 LAN 必须 `HOST=0.0.0.0` + `RIVEN_AUTH_TOKEN=<32+ hex>` |

带 token 启动后：landing / sources / overview?demo=1 / api/overview?demo=1 仍公开；其余 leadership 路径 401（除非带 `Authorization: Bearer <token>`）。已验证。

---

## 7. 测试套件

```bash
pnpm --filter @matrix-riven/collector-server test
# 52 files / 811 tests passed
```

---

## 8. 已知非阻塞项（Day-2 清单）

- `DEFAULT_ENDPOINT = http://192.168.22.88:8933` 在 `packages/shared/src/config.ts:88`（README:97 已自陈），客户端 install 时需要手编辑。dashboard 本身不依赖。
- HEAD 请求返回 405（应该是 200 / 405 + 空体）。监控 / link-checker 可能误判。
- `/retro?demo=1&range=24h` 还是渲染 7d demo 数据（demo fixture 是定死的 7d）。
- KPI 卡的 sparkline 是占位（landing copy 已自陈 "v0.2 接真数据"）。
- 演示热图当前是全 0（demo fixture 没填）。
- `?sort=alpha` 等 URL 参数不被 server 读取（sort 是纯客户端 JS）。

---

## 9. 18 轮对抗审计 trail (today)

| 轮 | 提交 | 关键修复 |
|---|---|---|
| 2 | `028e0cd` | 1.1 MB 成员 API → `allPrompts` 截到 12×800; /retro tab; README 重写 |
| 3 | (同) | landing "16 vs 17" 一处; /retro 孤儿修好 |
| 4 | `207a468` | PII 模式上提到 shared; e2e LLM-on 测试; POST /retro 405 |
| 4b | `a071a53` | 16 detectors 全文统一; 移除 Google Fonts @import |
| 5 | `d71038f` | POST /landing /sources /activity /insights → 405+headers |
| 6 | `d3f9a1a` | landing sparkline 文案诚实化; Activity/Insights tab 加 "· soon" |
| 7 | `f64e16b` | sources detector IDs 紧凑 1-16; 去掉 LLM Status 公开链接; demo 邮箱用 example.com |
| 8 | `74f61fa` | /api/members/:id, /api/projects/:name 支持 ?demo=1 |
| 9 | `2586c28` | 每个 demo 成员有自己的 MemberDetail（与 top-level 一致） |
| 10 | `898edcb` | 每个 demo 项目有自己的 ProjectDetail |
| 11 | `54343b2` | demo trend7d 校准（blake 不再 "加速"；devops 不再 "稳步推进"）; casey llmWeekly 修对项目 |
| 12 | `0f1908e` | dana today.sessions 与 lastSessionAt 对齐 |
| 13 | `730829f` | blake today.sessions == trend7d[6] |
| 14 | `a40685b` | **P0**: 成员 tile click 邮箱→local-part 契约; pollSO 传 ?demo=1 |
| 15 | `7479c2a` | **P0**: pollOverview / /people / /projects / nav 全部 demo-aware |
| 16 | `f4dcc36` | **P0**: /api/overview?demo=1 加 _html; /retro?demo=1 走 demo 数据 |
| 17 | `4797486` | propagateDemo 选择器 `a[href^="/"]`，覆盖 retro 返回链接 |
| 18 | (clean) | 无 P0/P1，仅 3 个 P2 defer |
| post-18 | `7eb0c21` | POST /people /projects → 405 (handover 冒烟漏网) |

合计：8 个 P0、20+ 个 P1、800+ 测试 → 811。
