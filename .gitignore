node_modules/
dist/
*.log
.DS_Store
coverage/
*.tsbuildinfo
.riven/
packages/*/.riven/
# Legacy (TeamBrain-era) data dirs — kept ignored during the deprecation window.
.teamagent/
packages/*/.teamagent/
.env
.env.local
.superpowers/

# Per-machine Claude Code overrides — .claude/settings.json (no .local) can
# still be committed for shared team settings; only the .local one is private.
.claude/settings.local.json

# viki (local knowledge graph harvested from this dev's CC sessions —
# strictly per-machine, may contain session summaries / corrections / etc.)
.viki/

# Smoke + QA evidence artifacts (manual curl dumps from launch-readiness rounds)
tmp_*.html
tmp_*.json
.tmp_*.html
.tmp_*.json
.qa-*.html
.qa-*.body.html
.em-*.html
.smoke-*.json
.smoke-cache/
.deploy/
0jingtongMatrix-Riven.claudeworktreesenumerated-roaming-engelbart.*.html
